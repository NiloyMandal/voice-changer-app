# Backend runtime settings
VOICE_SAMPLE_RATE=16000
VOICE_CHANNELS=1
VOICE_BLOCK_SIZE=256

# Optional audio device indices (set only if needed)
# VOICE_INPUT_DEVICE=0
# VOICE_OUTPUT_DEVICE=1

# Inference backend: torch | onnx
VOICE_INFERENCE_BACKEND=torch
